#init
c=[]
i=0

#start: supplied by casf.lambdas module
c.append(start)
c.append(f(c[i]))
format.readable.form5()(pd.DataFrame(data=c))

#sequence append
i+=1
c.append(f(c[i]))
format.readable.form5()(pd.DataFrame(data=c))

#2nd order sequence frame (notice/remind : no mp for assign?!)
fr2.assign(lam1 = lambda x: j(x[0])).applymap(format.readable.big5)
